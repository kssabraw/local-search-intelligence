# HANDOFF

Current-state handoff for the Local Search Intelligence Platform. Pairs with `CLAUDE.md` (durable project context + rules) and `docs/AUTHORITATIVE-ARTIFACTS.md` (recovered source-of-truth artifact registry).

## Latest — research ANALYSIS LAYER built + applied: first findings from the Full Panel (2026-09-19)

The first analysis pass over the completed panel — **no paid call, no methodology change, no data mutation** (read-only views). Turns the raw outcome panel into research findings and produces the evidence for the AIO-widen go/no-go.

- **Analysis layer (PR on `claude/intelligent-shannon-0292rm`, draft).** Migration `027_analysis_layer_v0_1.sql` adds an `analysis` schema of pure `CREATE OR REPLACE VIEW`s + an immutable `analysis.norm_domain()` (mirrors `collector/normalize.py` byte-for-byte): `observation_dim`, `market_eligible_points`, `coverage_summary`, `maps_entity_dominance`, `organic_domain_dominance`, `ring_profile`, `maps_center_retention` (distance-decay), `maps_organic_overlap`, `aio_overview_prevalence`, `aio_organic_source_overlap`. Methodology-faithful by construction: **no composite score, missing ≠ zero** (eligible-point denominators), outcome-only, canonical grains (Maps→resolved `business_location`; Organic→normalized domain). Idempotent + deploy-cheap (no materialized views; the Railway entrypoint re-runs `02[0-9]_*` every deploy). Design of record: `docs/design/analysis-layer-v0_1.md`. **Applied to production** (read-only, additive); the deploy re-applies it.
- **Validated offline:** `scripts/validate_analysis_views.py` (43 checks, ALL PASS) builds a hand-computable panel through the **production write path** (`collector.repository.Repo` → real entity resolution) on ephemeral pgvector and asserts every view. `pytest` 129 + `validate_migrations` still ALL PASS.
- **Notebook:** `analysis/fullpanel_202609_g13e_findings.ipynb` (+ `analysis/README.md`) renders the headline findings/charts; parameterized by `SUPABASE_DB_URL`, sets `statement_timeout=600s` (the 60 s pooler/MCP window is too small for a 1.4 M-row scan).
- **Headline findings (computed on production, `FULLPANEL-202609-G13E` + `AIO-20260918`):**
  - **Coverage:** 119,000 / 119,000 eligible observations returned (100%).
  - **Distance-decay (Maps, "near me"):** center Local-Pack retained ~18–22% @3 mi, ~7–9% @4 mi, ~3–4% @5 mi (consistent across locksmith/urgent-care/Chinese-restaurant) — steep proximity decay; validates the 13-point grid's resolution.
  - **Maps↔Organic overlap:** only ~16% of Local-Pack business websites also rank organically at the same point (locksmith Q1) — the surfaces reward largely different players.
  - **AIO prevalence (full panel):** a standalone AI Overview appears in **9.86%** of near-me SERPs — 4.3% ("near me") → 9.7% (in-[CITY]) → 11.7% ("best") → 13.7% (high-need). Rare + query-dependent.
  - **AIO↔organic redundancy:** when a triggered AIO cites web sources, they are ~100% already in the co-returned organic results (small n); AIO's *independent* signal is the GBP carousel (SearchViewer), not web citations.
- **Implication for the AIO-widen go/no-go:** AIO is rare (~10%) and its web layer is largely redundant with organic already collected; its only independent contribution is GBP-carousel embedding → argues for a **targeted** widen (high-AIO query classes / GBP-embedding capture) over the full ~148,750-task panel, or deferral. Panel-wide generalization (all 25 industries) runs from the notebook with the raised timeout. No paid gate opened.

**Owner decisions after reviewing the findings (2026-09-19) — TWO PAID STEPS QUEUED, NOT YET RUN (gates closed, pending explicit "go"):**
- **(1) Run the FULL AIO panel.** Owner wants the full AIO widen despite the redundancy finding (goal: measure AIO appearance/engine change over time). **Verified runnable on prod:** 25 industries × 10 `AIO_QUERY_V1` conditions × 595 eligible `GEOGRID13E_V1` points = **148,750 executable**, AIO surface already on `GEOGRID13E_V1`, all 250 industry-conditions seeded. Cost ≈ **$190/wave** at the measured 1,280 µUSD/task (possibly ~$130–190 — the `load_async_ai_overview` add-on's blended rate at the panel's ~10% trigger rate is still slightly uncertain). **Open decision: one-time baseline vs monthly cadence** (~$190/mo recurring — the longitudinal "AIO over time" goal favors monthly). To run (owner Railway action, from `main`): `RUN_PAID_AIO=1`, `AIO_ALL_CONDITIONS=1`, `AIO_POINTS_FULL=1`, `AIO_WORKERS=8`, redeploy → `AIO-<YYYYMMDD>` wave; re-close after. Large run (bigger than the 119k Maps+Organic panel) — expect several hours + resume passes (`AIO_RESUME=1`, zero re-pay). **Do NOT merge PR #46 mid-run** (auto-redeploy interrupts, resumes with no re-pay).
- **(2) Small AI Mode ↔ AIO alignment scan.** Directional probe: does AI Mode name/cite the same local players as the AIO? Scope ~30 AI Mode tasks (3 pilot industries × 5 pilot markets × center × 2 high-AIO conditions) + paired AIO-organic on the same cells → ~**$0.11** total (AI Mode 2,400 µUSD/task = $0.072; AIO-organic $0.038). **Caveat:** AI Mode is deferred (ADR-0008) with **no normalizer** — `aio_probe --mode ai_mode` only inventories into `parser_metadata`, so this is a **probe-level** comparison (from raw/probe metadata), not a normalized surface; a real AI Mode surface needs its own ADR-0005 gate + normalizer later. **AI Mode price** seeded `DFS_AIO_AIMODE_TASK_V1` = 2,400 µUSD (migration 024, single-wave measurement; may carry its own async add-on, unmeasured).
- **Longitudinal design note (for the "AIO/engine over time" goal):** AIO *prevalence* over time is already captured ~free — the monthly Maps+Organic Full Panel returns the `ai_overview` block presence in `organic.result` at the plain 600 µUSD price (no add-on); `analysis.aio_overview_prevalence` reads it. Cross-grid caveat: the two existing panels are on different grids (`MAPORG13_V1` vs `GEOGRID13E_V1`), so the clean prevalence trend starts from `GEOGRID13E_V1` forward. Engine-*content* drift (citation/business churn, body-vs-sidebar, GBP-embed rate, answer text) needs the loaded body and is a *sentinel* problem, but owner has chosen the full panel over a sentinel.

## Latest — GEOGRID13E_V1 paid collection EXECUTED: new-grid Full Panel + first AIO wave both COMPLETE; retention tooling added (2026-09-18)

The Maps/Organic collector was reconciled to the post-`026` grid and the first paid waves on `GEOGRID13E_V1` were run on production — **both evaluated COMPLETE**. All paid gates are re-closed.

- **Collector geometry reconciliation (PR #43, merged).** Migration `026` repointed `maps`/`organic` `surface_config` to `GEOGRID13E_V1`, but the collector still hardcoded the old grid: `pilot.PILOT_POINTS` listed the retired 1-mile ring (`N1/E1/S1/W1`) and `panel.GEOMETRY_CODE` was pinned to `MAPORG13_V1` — so the pilot dry-run raised `LookupError` on `N1` and **crashed the Railway deploy** (`set -euo pipefail`), blocking every paid block. Fix: `pilot`/`panel` now resolve the active grid from `manifest.surface_config` at run time (new `pilot.resolve_active_geometry` / `load_geometry_points` / `load_active_points`), so the collector auto-tracks a repoint and refuses a per-surface divergence — mirroring the AIO collector. Also fixed a **pre-existing panel cost-estimate bug** (`panel._expected_unit_microusd` was grabbing migration `024`'s 2,400 µUSD AI-Mode price; now scoped to the maps+organic SERP unit = 600 µUSD). Offline-validated (pytest 129 + all four ephemeral-pgvector validators); validator counts + the panel_run water-gate scope updated to the new grid.
- **Maps + Organic Full Panel on GEOGRID13E_V1 — EXECUTED + COMPLETE.** Wave **`FULLPANEL-202609-G13E`** (`PANEL_KIND=full_panel`, distinct code so new-grid jobs never mix with the old-grid `FULLPANEL-202609`): **130,000 planned → 119,000 executable / 11,000 structurally excluded**, **119,000/119,000 collected (100%)**, QA/Wave-Acceptance v0.1 = **COMPLETE** (valid-observation 1.0, job-accounting 1.0, normalization-parity 1.0, raw-integrity 1.0). **Realized $70.63** (~594 µUSD/task). The run spanned several resume passes — the decoupled collect phase timed out on not-yet-ready tasks, then re-fetched them by stored `provider_task_id` (PR #25 path), and a mid-run `main` merge (see below) redeployed the one-off, interrupting then resuming — all with **zero re-pay** (cost tracks 1:1 with collected observations). Interim per-pass evaluations read `failed` (partial coverage); the final evaluation is COMPLETE.
- **Graduated first paid AIO wave — EXECUTED + COMPLETE.** Wave **`AIO-20260918`** (`RUN_PAID_AIO=1`, created this session; graduated scope = 3×5 pilot cells × center × 2 core query families = **30 executable tasks**): **30/30 collected**, QA = **COMPLETE** (all gates 1.0). **Realized $0.0384.** **Async-billing measurement (the wave's purpose):** ~**1,280 µUSD/task** average ≈ **2.1× the 600 µUSD plain-SERP base** — i.e. the `load_async_ai_overview` add-on roughly doubled per-task cost. This is the number to size a full AIO panel against.
- **AIO findings (directional; n=5 triggered of 30 ≈ 17%, confirming ADR-0008 that a standalone AIO rarely fires for near-me local intent):**
  - **Body vs sidebar is carried by `aio.citation.citation_kind`:** `inline_link` (body) = local businesses named in the answer text (locksmiths, urgent-care brands, one restaurant); `reference_card` (sidebar source cards) = **aggregators (Thumbtack) + official brand/health-system sites (Mount Sinai / NYU Langone / CityMD / ExpressCare)**. The explicit `presentation_zone` column was null in this sample. **No social media** in any citation.
  - **Embedded GBP:** the NYC urgent-care AIO embedded a **20-business GBP carousel** (`embedded_gbp=true`, Google **SearchViewer** direct links) — incl. a few mis-localized LA entries (AIO conflating "Manhattan").
  - **AIO ↔ Organic overlap is tight:** every AIO sidebar source for NYC urgent care also ranks in the organic top ~14 (CityMD #1–4, Mount Sinai #5, NYU Langone #7, ExpressCare #9). Working hypothesis: winning a local AIO needs presence in **both** the GBP/Maps layer (named/embedded businesses) **and** the organic layer (cited sources).
- **Data-retention / cleanup tooling (PR #44, merged; NOT yet run).** `docs/design/data-retention-cleanup-v0_1.md` + `scripts/prune_superseded_normalized.py` (read-only by default; deletes only with `--execute` **and** `CONFIRM_PRUNE=1`; active-grid guard; scoped child→parent deletes; **disables the append-only `ops.reject_update_delete` USER trigger for the txn** while keeping FK triggers on) + `scripts/validate_prune.py` (ALL PASS). Prunes the rebuildable normalized layer of superseded `MAPORG13_V1` waves to reclaim ~**5–7 GB** (raw payloads in Storage + canonical entity graph are kept); the DB had grown to ~10 GB after two Full Panels. Reclaiming physical disk still needs `VACUUM FULL`/`pg_repack` after a prune.
- **Gates re-closed:** `RUN_PAID_PANEL=0`, `RUN_PAID_AIO=0`, `PANEL_KIND=auto`, `PANEL_WAVE_CODE` cleared (set with `skipDeploys=true`).
- **Two operational notes:** (1) **merging to `main` auto-redeploys the Railway service**, which interrupts a live paid one-off (it resumes with no re-pay, but avoid `main` merges mid-run). (2) **Data captured is OUTCOME-only** — rank / coverage / reach + the Maps-profile ride-alongs DataForSEO returns inline (rating, review count + distribution, categories, `is_claimed`, hours, photo count, price level, distance). There is still **NO enrichment/predictor data** (backlinks, on-page/technical, off-surface GBP audit, NAP/citations, review velocity); meaningful "what predicts visibility" correlation needs the enrichment stage (`docs/design/enrichment-pricing-probe-v0_1.md`), which is out of current scope and unrun.

## Latest — efficient unified geo-grid `GEOGRID13E_V1` ACCEPTED + applied to production (ADR-0009, migration `026`)

The active geometry for **both** Maps/Organic and AIO is now **`GEOGRID13E_V1`**: center + N/E/S/W @ **3/5 mi** + NE/SE/SW/NW @ **4 mi** (13 points). It **drops the low-yield 1-mile cardinal ring** and adds a 4-mile diagonal ring, decided on measured Full-Panel saturation (the 1-mile ring added only ~4 businesses across its 4 points; outer rings add ~4–7 each). It also **unifies AIO onto the same grid** (was `AIO9_V1`, 2.5/5), so every AIO observation is co-located with a Maps/Organic observation. The 200 new 4-mile diagonals were classified by the authoritative `SED_GEO_ELIGIBILITY_CENSUS_2025_V1` gate (TIGER 2025 AREAWATER water gate + US-state country gate; 185 eligible / 14 water / 1 outside-country). `MAPORG13_V1` and `AIO9_V1` are **retained as history**. Migration `026` is **applied to production and verified** (geometry_version 3 / geometry_point 35 / coords 1,750 → **1,595 eligible / 141 water / 14 outside**; GEOGRID13E_V1 = 650 / 595 eligible; maps/organic/aio all repointed). Design of record: `docs/adr/0009-unified-efficient-geo-grid.md` + `docs/design/unified-geo-grid-v0_1.md`. Consequence: Full Panel Maps+Organic executable is now **595 × 200 = 119,000** (was 118,000); AIO rides the same 595 eligible points. No paid call ran; gates closed. Reproducible tooling: `scripts/gen_diagonal_coords.py`, `scripts/run_water_gate.py`, `scripts/gen_migration_026.py`. Enrichment pricing-probe spec authored (`docs/design/enrichment-pricing-probe-v0_1.md`) — not yet run.

_Last updated: 2026-09-18 (GEOGRID13E_V1 paid Full Panel + first AIO wave COMPLETE + retention tooling — see the top *Latest* section). Prior (2026-09-16): methodology/design complete; Manifest v1.0 frozen executable; authoritative v0.1 contracts + Manifest + PRDs on `main` (PR #1). Physical schema split into ordered migrations `001`–`023` + Manifest v1.0/QA seeds, validated on local pgvector (1,100 → 1,000/91/9), merged. Stage-1 vertical-slice **Maps + Organic** collector built + validated offline (PR #10). Pilot finding: Maps zoom `17z` returned no results; locked to `14z` by owner-approved amendment (ADR-0006, migration `022`). Migrations `001`–`023` applied to the persistent PRODUCTION Supabase project (`wbqcvqxmhqyspgqsdpsm`), reconciling 1,100 → 1,000/91/9. **The first paid single-coordinate spike is now DONE and verified on PRODUCTION for BOTH surfaces (step 7 complete): Maps 10/10 businesses resolved by place_id, Organic 14 result rows → 9 web destinations resolved URL-first (0 business_locations), $0.0006 each. The bounded 3×5 Maps+Organic pilot (step 8) was EXECUTED on production and evaluated **COMPLETE** under QA/Wave-Acceptance v0.1 (step 9): wave `PILOT-3x5-20260914`, 1,368/1,368 executable returned (100%) + 192 structurally excluded, all rates 1.0, all 30 strata pass, 0 identifier splits, ~$0.8142 total. Steps 1–9 done. **Step 10 (Full Panel / Sentinel for Maps + Organic) is now in progress:** ADR-0007 + scheduler design signed off (PR #18), the manifest-driven wave generator + set-based dry-run (PR #19) and the Standard decoupled adapter + two-phase panel runner (PR #20) are built and offline-validated (no paid call); in-scope executable counts Full Panel 118,000 / weekly Sentinel 4,400 Maps+Organic jobs. The cadence driver + `RUN_PAID_PANEL` gate + Railway wiring (step 3) are built and offline-validated (PR #22 merged). **The graduated first paid Sentinel wave (step 4) was EXECUTED on production and evaluated COMPLETE:** wave `SENTINEL-2026W38` (Standard decoupled method), 5,200 planned → 4,400/4,400 executable returned (100%) / 800 structurally excluded, every QA gate 1.0, all 100 strata pass, 0 failed / 0 quarantined / 0 identifier splits, **$2.6304**; `RUN_PAID_PANEL` re-closed after. **The first monthly Full Panel (`FULLPANEL-202609`) was then EXECUTED on production and evaluated COMPLETE (2026-09-16): 130,000 planned → 117,994/118,000 executable returned (99.995%) / 12,000 structurally excluded, entity graph clean (0 duplicate domains/URLs), 0 real provider failures, 6 tasks accounted `collect_timeout` (paid at submit, uncollected — within COMPLETE tolerance), $70.29 realized; all paid gates re-closed. Two improvements shipped while running it (no methodology change, zero re-pay): parallel collect (PR #24) and direct `task_get`-by-id resume (PR #25). Step 10 is COMPLETE.** **Stage 2 (AIO) has now started: the ADR-0005 capture-feasibility probe was built (PR #27) and EXECUTED on production (2026-09-16) — wave `AIOPROBE-AIOPROBE_V0-20260916`, 30/30 AI-Mode tasks triggered (rate 1.0), $0.072 (2,400 µUSD/task, seeded as `DFS_AIO_AIMODE_TASK_V1` in migration 024), gate re-closed. Result: 8/9 AIO-PRD capture fields CAPTURABLE (incl. element rectangles, citations, SearchViewer/Maps GBP destinations); only the structured local-business-card MODULE is NOT_OBSERVABLE on AI Mode (it is an AI-Overview-in-organic surface — an open owner decision). See `docs/design/aio-schema-decision-v0_1.md`. The AIO schema/normalizer commit is deferred pending that sign-off.** ChatGPT remains behind its ADR-0005 capture probe. **Step 10 (Maps + Organic Full Panel / Sentinel) is COMPLETE. Stage 2 (AIO) BUILD is COMPLETE:** both capture probes EXECUTED; ADR-0008 fixes the surface of record = the AI Overview in the organic SERP (AI Mode deferred); migration `025` applied on production (aio.* schema reconciled + provider profile repointed `DFS_AIO_V1`→`DFS_AIO_V2`, organic ai_overview); the two-track `parse_aio` normalizer (PR #34), the gated `aio_driver` + `RUN_PAID_AIO` gate + Railway wiring (PR #36), and the QA cost-reconciliation price-scope fix (PR #37) are all merged to `main` + offline-validated (pytest 128, all validators PASS). **No paid AIO collection has run; all paid gates closed.** The remaining Stage-2 step is the graduated first paid AIO wave (~30 tasks) on explicit owner "go", to measure the `load_async_ai_overview` add-on billing. ChatGPT (Stage 3) stays behind its own ADR-0005 probe.**_

## Stage 2 (AIO) — BUILD COMPLETE + graduated first paid wave EXECUTED COMPLETE (2026-09-18; see top *Latest*)

**Status summary.** Everything AIO needs to collect is built, merged to `main`, offline-validated, and the **graduated first paid wave has now run COMPLETE** (`AIO-20260918`, 30/30, $0.0384; async add-on measured ~1,280 µUSD/task ≈ 2.1× the SERP base). Build components: the surface decision (ADR-0008), the applied schema + provider repoint (migration `025`), the two-track `parse_aio` normalizer (PR #34), the gated `aio_driver` collection driver + `RUN_PAID_AIO` gate + Railway wiring (PR #36), and the QA cost-reconciliation price-scope fix (PR #37). All paid gates re-closed (`0`). **Next AIO step is a widen go/no-go** (full 13-point × 10-condition panel, ~148,750 executable). ChatGPT (Stage 3) stays behind its own ADR-0005 probe. Prior status detail (pre-wave) retained below. AI Mode is deferred; ChatGPT (Stage 3) stays behind its own ADR-0005 probe. **Geometry update (ADR-0009, migration `026`) — supersedes the `AIO9_V1` references in this section:** the AIO surface is now unified onto the efficient **`GEOGRID13E_V1`** 13-point grid (center + N/E/S/W @3/5 mi + NE/SE/SW/NW @4 mi; was `AIO9_V1`, 9-point 2.5/5) — see the *Latest* section at the top. AIO collection resolves geometry via `surface_config`, so it follows the repoint automatically; the `aio_run`/`aio_driver` point vocabulary was updated from the 9-point to the 13-point set (`AIO_POINTS_FULL`, `--points-full`). The graduated first wave is center-only, unaffected; a full AIO panel is now 13 points / 595 eligible (was 9 / 410).

- **ADR-0005 gate-1 for the AIO surface.** Built offline (PR #27): `collector/inspect_aio.py` (schema-agnostic, pure-Python capability inspector), `collector/aio_probe.py` (3×5 pilot-cell sweep + `summarize_capture` roll-up + `RUN_AIO_PROBE` gate), `spike.py` aio probe-only path (requests `calculate_rectangles`, records the inventory + truthful per-task cost), fixtures + `scripts/validate_aio_probe.py`. Rides the already-seeded AIO manifest (`aio` surface + `DFS_AIO_V1` AI-Mode profile + `AIO9_V1` geometry materialized across the pilot markets + `AIO_QUERY_V1` conditions).
- **Executed on production (owner "go"):** wave `AIOPROBE-AIOPROBE_V0-20260916` — 30/30 AI-Mode tasks probed, **30/30 triggered (rate 1.0)**, **$0.072** (2,400 µUSD/task — measured AI-Mode price, seeded `DFS_AIO_AIMODE_TASK_V1` in migration `024`). 30 immutable observations + 30 cost events, **0 `aio.*` normalization rows**, gate re-closed (`RUN_AIO_PROBE=0`).
- **Findings (see `docs/design/aio-schema-decision-v0_1.md`):** the AI-Mode endpoint returns AI-Overview-shaped content (`ai_overview` / `ai_overview_element` ×207 / `ai_overview_reference` ×547 / `images_element` / `link_element`); **no `local_pack`/`map`/`knowledge_graph` types**. **8/9 AIO-PRD capture fields CAPTURABLE** (answer text, element structure, **element rectangles** — `calculate_rectangles` honored, citations, reference-vs-link, business position, embedded GBP, SearchViewer destinations); **`local_business_cards` NOT_OBSERVABLE** (no card module on AI Mode → `provider_not_observable`).
- **GBP detection in AI Mode (grounded):** businesses appear as `ai_overview_reference` items whose destination is a Google **SearchViewer** deep link (`/searchviewer/…?svid=…`); the `svid` packs a Knowledge Graph MID (`/g/…`). Deterministic mechanics shipped + unit-tested in `collector/aio_destination.py` (`classify_destination` → PRD §69 `destination_type`; `decode_searchviewer_svid` → KG MID). The normalizer will resolve AIO businesses by KG MID (`google_kg_mid` external identifier), joinable to the Maps `place_id` graph later.
- **AI-Overview-in-organic probe EXECUTED (2026-09-16; PR #29 + follow-ups):** wave `AIOPROBE-ORG-AIOPROBE_V0-20260916` — 15 organic tasks, **$0.018**, gate re-closed. Finding: a **standalone AI Overview rarely triggers** for near-me local-commercial intent (1/15 top-level `ai_overview`, returned as an **async stub**; 10/15 carried AIO content only inside People Also Ask). Google shows the **Local Pack** for these queries. The first roll-up looked like `local_business_cards` CAPTURABLE on organic, but that was the inspector conflating the ever-present SERP `local_pack` with an AIO card; the inspector now runs **`scope="ai_overview"`** for organic mode (detection restricted to the ai_overview element subtree), which removes the conflation — no distinct AI-Overview local-card module was observed on either surface for this intent.
- **DECISION — ADR-0008 (owner-signed, PR #31): the AIO surface of record is the AI Overview in the organic SERP, NOT AI Mode.** The AIO PRD (§1/§55/§79) requires *prevalence* ("whether an AIO appears") and *placement* (left/right, above-the-fold, position vs organic rankings / Local Pack) — both **SERP-only** phenomena that AI Mode (always-on, no SERP position) cannot provide. AI Mode is a different Google product (conversational tab) — **deferred** as a separate future surface (sibling to Stage-3 ChatGPT), its own ADR-0005 gate if pursued; **not** part of Stage 2. Capture per observation: prevalence (`aio_triggered`, absence = valid negative), the body (sidebar sources + inline-vs-reference citations §18 + rectangles + GBP/KG-MID destinations), **SERP placement** (rank among blocks, preceding blocks, rectangle → top vs middle), and the co-returned organic rankings + Local Pack (H3). **PAA-AIO excluded**; local-card *module* → `provider_not_observable`.
- **APPLIED to production — migration `025` (PR #32):** `aio.*` schema reconciled (sidebar-card fields, `citation_kind`/`is_reference`, element + block rectangles, SERP-placement columns; `google_kg_mid` is free-text, no DDL); AIO provider profile **repointed `DFS_AIO_V1` (ai_mode) → `DFS_AIO_V2` (organic `/v3/serp/google/organic` + `load_async_ai_overview` + `calculate_rectangles`, `{lat},{lon},200`, depth 10)**, `DFS_AIO_V1` kept as history; `aio` `surface_config` now resolves to `DFS_AIO_V2` (verified live). Migrations `001`–`025` applied+reconciled on production; provider_profile count 6. Design of record: `docs/adr/0008-aio-organic-overview-surface.md` + `docs/design/aio-organic-overview-schema-v0_1.md`.
- **Shipped helpers (offline):** scoped inspector `inspect_aio_capture(..., scope="ai_overview")`; `collector/aio_destination.py` (`classify_destination` → §69 `destination_type`; `decode_searchviewer_svid` → KG-MID); opt-in async expansion `--load-async-aio` / `AIO_PROBE_LOAD_ASYNC=1`. Session paid total for both probes: **$0.090**; all gates closed.
- **Cost impact:** AIO drops to ~600 µUSD/obs + async add-on (only when a standalone AIO loads) vs 2,400 µUSD AI-Mode — ~50–75% cheaper per observation; the same organic call also returns organic rankings + Local Pack. No AIO collection has run; no change to Maps/Organic costs.
- **`parse_aio` normalizer BUILT + offline-validated (no paid call).** `collector/parse_aio.py` (pure, deterministic, no LLM) parses one organic-AIO response two-track: (1) reuse `parse_organic`/`write_organic` for the organic results + Local-Pack context (H3 within-observation join); (2) parse the `ai_overview` subtree (standalone only, PAA excluded) into `aio.*`. `spike.finalize_aio` (dispatched by `run_spike` for the `aio` surface) writes ONE `ops.observation` carrying both tracks; `Repo.write_aio` fills `aio.observation` (prevalence `aio_triggered` — false is a valid negative, presentation form `standalone`/`async_stub`/`absent`, `async_ai_overview_loaded`, SERP placement `serp_rank_absolute`/`serp_position`/`serp_preceding_block_count`+types/rectangle), `aio.presentation_unit` (+ element rectangles), `aio.source_occurrence` (sidebar cards, URL-first→domain resolution, deduped with organic web entities), `aio.citation` (`inline_link` vs `reference_card` + `is_reference` §18), and `aio.business_appearance` + `aio.destination` for GBP/SearchViewer references, resolved by KG-MID (`resolve_aio_business`, `google`/`google_kg_mid`, → `business_location`, place_id-graph-joinable). Local-business-card MODULE stays unwritten (`provider_not_observable`). Offline-validated on ephemeral pgvector (`scripts/validate_aio_normalize.py`: loaded AIO + local_pack; async stub; no-AIO negative; clean entity graph; idempotent resume — ALL PASS) + `tests/test_parse_aio.py` (pytest 117, providers mocked). No paid AIO collection has run; `RUN_AIO_PROBE` and all paid gates stay closed. Then a paid AIO run stays gated on explicit owner "go". ChatGPT (Stage 3) stays behind its own ADR-0005 probe.
- **AIO paid driver + `RUN_PAID_AIO` gate BUILT + offline-validated (no paid call).** The gated cadence driver for AIO collection, analogous to `panel_driver`/`RUN_PAID_PANEL`: `collector/aio_run.py` (the AIO job matrix `expand_aio_matrix` — `AIO_QUERY_V1` × `AIO9_V1`, with a GRADUATED default scope = 3×5 pilot cells × center × the 2 core query families ≈ 30 tasks — + `build_aio_runner`, which reuses the SAME validated `pilot.PilotRunner` parametrized for the `aio` surface, so there is **no parallel per-surface collection stack**) and `collector/aio_driver.py` (deterministic per-day wave code `AIO-<YYYYMMDD>`, `--resume`, water-gated dry-run default, run → `pilot.evaluate_wave` persisted, non-COMPLETE → non-zero exit). `pilot.PilotRunner`/`plan_dry_run`/`evaluate_wave` were minimally + additively extended: a `treatment_set_code`/`wave_kind`/`component_name`/`wave_code_prefix` seam (Maps/Organic defaults unchanged) and an `aio` branch in the normalization-parity check (an `aio.observation` is the aio surface's normalized row). **KG-MID split-safety:** unlike a cell-local Maps `place_id`, an AIO business KG-MID can recur across cells, so `resolve_and_assert_aio_business` now serializes creation with a **separate advisory-lock namespace** (`prelock_business_kg_mids` + `_lock_business_create`, acquired ascending AFTER the web-domain shards for a global web→business order) — proven by a 5-worker concurrent validator where one business in all 15 cells resolves to **one** `business_location` entity (0 identifier split). **Gate: a NEW `RUN_PAID_AIO`** (default `0`/closed, independent of `RUN_PAID_SPIKE`/`RUN_PAID_PILOT`/`RUN_PAID_PANEL`/`RUN_AIO_PROBE`), checked before any DB connection; wired into `scripts/railway_run.sh` (dry-run on deploy + paid run only when `RUN_PAID_AIO=1`, `AIO_*` vars). Offline-validated: `scripts/validate_aio_run.py` (clean wave → QA COMPLETE; concurrent 5-worker run → 0 KG-MID split / 0 duplicate domains-URLs; idempotent resume; water gate; gate refusal) + `tests/test_aio_driver.py` (pytest 128, providers mocked). No paid AIO collection has run; all paid gates stay closed. **The load_async_ai_overview add-on billing (per-task vs per-trigger) is unconfirmed — the graduated first paid wave must MEASURE it before any wider run.** Next paid AIO run stays gated on explicit owner "go".
- **QA financial-reconciliation price scoping — FIXED.** The `expected_unit_microusd` query (`collector/pilot._financial_reconciliation`) previously selected the single most-recent versioned DataForSEO price with no surface filter, so migration `024`'s AI-Mode price (2,400 µUSD, later `effective_from`) shadowed the Maps/Organic 600 µUSD baseline (`023`) for every wave (`validate_pilot` failed `expected 600, got 2400`). Now scoped to **the wave's own provider profiles**: a wave whose profiles use the `ai_mode` endpoint reconciles against the AI-Mode price; every other wave (Maps, Organic, and the organic AI Overview `DFS_AIO_V2`) reconciles against the SERP-standard 600 µUSD price — matched by endpoint family, no schema change, both prices left active (the unit amount still comes from the versioned registry, never hard-coded). Offline-validated: `scripts/validate_qa_price_scope.py` (maps/organic/AIO-organic → 600; AI-Mode history → 2,400) + `validate_pilot` now ALL PASS.

## Stage-1 pilot findings

- **Maps coordinate zoom locked `17z → 14z`** (owner-approved, ADR-0006, migration `022_amend_maps_zoom_14z.sql`). Manifest v1.0 tagged provider settings `PENDING_EXACT_ENDPOINT_LOCK`; the pilot locked them. Evidence (MKT008 center, "locksmith near me", depth 10): 17z→0, 15z→6, **14z→10**, 12z→10; center-vs-5mi-north at 14z returned different local businesses, confirming per-coordinate proximity signal. Universe unchanged; only the Maps provider profile is versioned (`DFS_MAPS_V1` 17z retained as history; `DFS_MAPS_V2` 14z is live).
- **`40102 "No Search Results"` is now classified as a valid empty observation** (`returned`, 0 results), not `provider_failure` (missing ≠ zero). See `collector/parse_maps.py`.
- **Spike mechanics validated end-to-end** on `lsi-dev`: at 14z the center coordinate returned a full Top-10 with 10/10 businesses resolved to canonical entities by place_id; cost ledger recorded (~$0.0006/call). Total pilot-probe spend ≈ $0.0024.
- **Organic capture gate PASSED (no amendment needed).** A `--probe-only` capture probe (ADR-0005) at the same coordinate confirmed the shipped Organic `location_coordinate` form `{lat},{lon},200` returns a full SERP: DataForSEO `20000 Ok`, 14 result blocks (`local_pack`, `organic`, `people_also_ask`, `related_searches`), coordinate applied (decoded `uule` = MKT008 center). The `17z` problem was Maps-specific; Organic needs no coordinate change. Remaining Organic work is engineering only — build the `organic.*` parser/normalizer — not methodology.
- **Capture-feasibility probe tool:** `collector/spike.py --probe-only` (surface-agnostic; wave `PROBE-<surface>-*`) records provider status + result-item count + `check_url` into `ops.observation.parser_metadata` without surface-specific normalization. Reusable for the AIO and ChatGPT ADR-0005 gates.
- **Organic parser/normalizer built + offline-validated (engineering, no methodology change).** `collector/parse_organic.py` + `resolve_organic_item` + `Repo.write_organic`/`resolve_and_assert_organic` mirror the Maps path on the shared foundation; the spike dispatches on `surface_code`. Every SERP block (organic, local_pack, PAA, related_searches) is preserved as an `organic.result` row with its `result_type`/`rank_absolute`; only organic web destinations become `core.observed_object` and resolve **URL-first then domain** to `core.web_url`/`core.web_domain` (never `business_location`, per contract §14). `scripts/validate_spike.py` now runs the full Maps **and** Organic parse→normalize→resolve→cost path (+ idempotency) against synthetic fixtures on an ephemeral pgvector Postgres — all pass, no paid call. The live 3×5 Maps+Organic pilot remains gated on owner confirmation for the first paid call.
- **One-command migration apply added.** `scripts/apply_migrations.py --dsn "$SUPABASE_DB_URL"` applies `001`–`NNN` in order (each file its own transaction, stop-on-error), then prints the reconciliation + universe counts; `--check-only` re-verifies without writing, `--dry-run` lists files, `--force` re-applies over an existing schema. Uses `psycopg` (no external `psql`/CLI). Also corrected a stale expected count in `scripts/validate_migrations.py` (migration `022` added `DFS_MAPS_V2`, so `manifest.provider_profile` is 5, not 4). The Railway one-off `scripts/railway_run.sh` also applies migrations on deploy via `psql` against `$SUPABASE_DB_URL`.
- **Migrations applied to PRODUCTION + verified (2026-09-14).** `001`–`023` are applied to the persistent production Supabase project `wbqcvqxmhqyspgqsdpsm` (not a dev branch — the `lsi-dev` branch was torn down). Reconciliation confirmed on production: 1,000 eligible_land / 91 structural_water_exclusion / 9 outside_country_exclusion (1,100 total), 25 industries, 50 markets, 600 treatments, 5 provider profiles, 80 QA rules, 1 frozen methodology. Verified both via the Railway deploy log and an independent Supabase query.
- **First paid single-coordinate spike on PRODUCTION — DONE + verified (2026-09-14), step 7 complete.** Ran via the Railway service (`RUN_PAID_SPIKE=1`, then reset to `0`) at the default pilot coordinate `MKT008_MAPORG_C` (Vancouver WA center, `eligible_land`), IND010 "locksmith near me", depth 10. Total paid spend **$0.0012** (2 × $0.0006).
  - **Maps** (`SPIKE-20260914T054319`, obs `04a4155a-26dc-4dfe-afd1-026364d01f34`): `observation_state=returned`, full **Top-10 at 14z**, 10 `maps.result` rows, 10 `core.observed_object`, **10/10 resolved to distinct `core.business_location` by place_id**, 3 immutable `ops.raw_blob` (request+post+get), `ops.cost_event` 600 µUSD `maps_spike_task`. Verified by independent Supabase query (rank-1 "Locksmith Plus, Inc.").
  - **Organic** (`SPIKE-20260914T054830`, obs `b408c34a-d910-4cee-9c34-719ca2f0ce76`): `returned`, **14 `organic.result` rows preserving every block** (9 organic + 3 local_pack + 1 people_also_ask + 1 related_searches), **9 organic web destinations → 9 `core.observed_object` → 9/9 resolved to `core.web_url` (8 distinct `core.web_domain`), 0 `core.business_location`** (contract §14 held: Organic never mints a business), 3 raw blobs, cost 600 µUSD `organic_spike_task`. Coordinate form `{lat},{lon},200` (Organic, unchanged); endpoint `/v3/serp/google/organic/task_post`.
  - **Two PRODUCTION infra/config fixes were required first** (both surfaced *before* any paid call — $0 wasted, immutable-raw-first ordering + transaction rollback protected the run): (1) `SUPABASE_URL` on Railway held a malformed value (unresolvable host) → reset to canonical `https://wbqcvqxmhqyspgqsdpsm.supabase.co`; (2) `SUPABASE_SERVICE_ROLE_KEY` was a wrong/stale key (Storage reported `AccessDenied — signature verification failed`, `role: anon`, from the torn-down `lsi-dev` branch) → **replaced by owner with the production project's `service_role` key**. Collector hardened so this class of failure is self-diagnosing (**PR #11, merged to `main`**: `StorageConfig` strips whitespace/trailing-slash on URL/key/bucket; `raw_store` surfaces the Storage response body instead of a bare `raise_for_status`; 29 offline tests pass). The post-merge Railway deploy from `main` is green and now runs the hardened collector; `RUN_PAID_SPIKE=0` (gate closed).

## Where we are

The research methodology is complete. Do **not** reconstruct the plan of record, domain model, physical schema, QA contract, or Manifest from scratch. The repo scaffold was created after those artifacts existed, so some early repo drafts contain stale pre-freeze language.

**Repository state:** the authoritative artifacts below are now on `main`, byte-verified against Drive on import (the two geo CSVs additionally SHA-256-checked against `manifest/SED_Geo_Eligibility_Report_v1_0.json`). The import also reconciled two stale short drafts — `docs/contracts/physical-schema-contract-v0_1.md` (Maps/Organic-only → full 10-schema model) and `docs/contracts/qa-wave-acceptance-contract-v0_1.md` — to their full authoritative versions, per the `docs/AUTHORITATIVE-ARTIFACTS.md` conflict rule. No methodology change. (The same content was briefly staged in the AR-Tools monorepo under a `sed/` prefix; that PR was closed unmerged — this repo is the single home.)

Repository-native planning context:
- `CLAUDE.md` — implementation plan of record / durable project rules.
- `CONTEXT.md` — domain glossary/model context.
- `docs/adr/` — load-bearing architecture decisions.
- `docs/AUTHORITATIVE-ARTIFACTS.md` — registry of the authoritative pre-repo research artifacts and conflict rules.

Authoritative artifacts now mirrored in repo (byte-verified against Drive on import; see `docs/AUTHORITATIVE-ARTIFACTS.md` for the full registry):
- `docs/contracts/physical-schema-contract-v0_1.md` — full physical schema contract v0.1 (replaced the earlier short Maps/Organic-only draft).
- `supabase/schema/physical-schema-v0_1.sql` — physical schema SQL migration v0.1 (the authoritative full-schema source; split into `supabase/migrations/` per the build sequence).
- `docs/contracts/qa-wave-acceptance-contract-v0_1.md` — full Operational QA / Wave Acceptance contract v0.1 (replaced the earlier short draft).
- `docs/contracts/qa-rules-v0_1.json` + `supabase/seeds/qa_rules_v0_1.sql` — machine-readable `qa_rules` seed (24 rules) + SQL seed.
- `docs/contracts/qa-wave-acceptance-rules-v0_1.json` — machine-readable wave-acceptance rules (58 rules, PRE/COL/NOR/RES/ENR/CST/ANA stages).
- `docs/contracts/job-generator-contract-v0_7.json` — collection-job generator contract v0.7.
- `manifest/SED_Collection_Manifest_v1_0.json` + the four geography inputs (`SED_Coordinates_GeoEligible_v1_0.csv`, `SED_Geo_Eligibility_Classification_v1_0.csv`, `SED_Geo_Eligibility_Report_v1_0.json`, `SED_Geo_Source_Manifest_v1_0.json`) — the frozen Manifest v1.0 + SHA-256-verified geography.
- `docs/prd/` — the four governing PRDs (unified parent + Maps/Organic + AIO + ChatGPT).

Authoritative Drive artifacts (source-of-truth IDs; the ones above are now mirrored in-repo):
- Physical schema contract v0.1 — `1QoAiU5Kqce666orez0TCzbBway460Zpg`.
- Physical schema SQL migration v0.1 — `1whsoY_XVYBjgmQrzlpl-6dcq10z5kASG`.
- Operational QA / Wave Acceptance Contract v0.1 — `1XSSm3UpjEsil6wAUEYXZRxVlhMUxQSRZ`.
- Manifest v1.0 JSON — `1xl5sGm9fCdz-aLFEKX2v8ETpodgxfLsh`.
- Authoritative research handoff — `1GrlD5M1WcAmmdaf8PCYCpj6P4V87Ezm1` (not yet mirrored in-repo).

## Build status

**There is no remaining methodology/sign-off blocker before engineering.**

Manifest v1.0 geography is resolved before first live scientific collection:
- 1,100 total spatial coordinates evaluated;
- 1,000 eligible land;
- 91 structural-water exclusions;
- 9 outside-country exclusions;
- 0 manual review;
- 0 configuration failures.

Executable workload after geography exclusions (all four surfaces):
- Full Panel: **258,000 jobs/month**;
- weekly Sentinel: **9,800 jobs**;
- bounded 3-industry × 5-market pilot: **3,048 jobs across Maps, Organic, AIO, and ChatGPT**.

**Maps + Organic in-scope subset (Step 10, verified against the production manifest):** Full Panel **118,000** executable / 12,000 structurally excluded / 130,000 planned (~$70.80 @ 600 µUSD/task); weekly Sentinel **4,400** executable / 800 excluded / 5,200 planned (~$2.64). MAPORG13_V1 = 590 eligible / 55 water / 5 outside-country coordinates across the 50 markets; 110 eligible in the 10 Sentinel markets.

The old 280,000 / 11,200 counts are pre-geography planning counts, not the final executable counts.

## Infra

- **Supabase** project `local-search-intelligence` (ref `wbqcvqxmhqyspgqsdpsm`, org `rzgbmlgbileospunrxaf`, `us-west-1`, **PG 17.6**) — migrations `001`–`026` are **applied and reconciled** here (1,750 → 1,595/141/14 after `026` accepts `GEOGRID13E_V1`; the frozen `MAPORG13_V1`/`AIO9_V1` universe was 1,100 → 1,000/91/9). This persistent production project is the live DB; the earlier `lsi-dev` branch was torn down. Resolve refs by name at use time (`list_projects`); never hardcode.
- **Railway** project `local-search-intelligence` (`production` env) has a **live service** that runs the one-off `scripts/railway_run.sh` (Dockerfile) — it migrates + reconciles + dry-runs on deploy, and makes the single paid call only when `RUN_PAID_SPIKE=1`. `SUPABASE_DB_URL` points at the production **session pooler** (`postgresql://postgres.wbqcvqxmhqyspgqsdpsm:<pw>@aws-0-us-west-1.pooler.supabase.com:5432/postgres`). NOTE: the pooler is required because Railway egress is IPv4-only while Supabase's direct `db.<ref>.supabase.co` host is IPv6-only; and the pooler username must be ref-qualified (`postgres.<ref>`), or auth fails as `user "postgres"`.
- **Secrets set (Stage-1):** `DATAFORSEO_LOGIN`, `DATAFORSEO_PASSWORD`, `SUPABASE_DB_URL`, `SUPABASE_SERVICE_ROLE_KEY`, `SUPABASE_URL` are configured on the Railway service. The Gemini and ChatGPT-vendor keys are **not** set (not needed until enrichment / ChatGPT, both out of current scope). Secrets live only in Railway/Supabase secret management — never in this public repo or in chat.

## Governing build sequence

1. Reconcile the repo implementation context to `docs/AUTHORITATIVE-ARTIFACTS.md`; do not re-derive methodology.
2. Apply/split the authoritative physical Supabase/Postgres schema v0.1 migration in an isolated development environment.
3. Keep research schemas private; configure immutable/content-addressed raw Storage.
4. Seed the frozen Manifest v1.0 and QA contract/rules.
5. Reconcile database keys/counts against Manifest v1.0 before provider collection.
6. Implement provider adapters, immutable raw retention, parsing/normalization, entity resolution, cost ledger, retries/quarantine, and QA evaluation.
7. Run a single-coordinate vertical-slice spike end-to-end.
8. Execute the bounded 3×5 pilot from the frozen v1.0 executable matrix (3,048 jobs across all four surfaces).
9. Evaluate COMPLETE/PARTIAL/FAILED/QUARANTINED under the QA contract. Production promotion requires COMPLETE.
10. After pilot validation, proceed to Full Panel/Sentinel operation without silently changing methodology.

## Hard methodology boundary

Implementation may not silently change research population, treatment, estimand, cadence, geometry, result depth, replicate behavior, missingness semantics, or enrichment eligibility. A genuine change to one of those requires an explicit methodology amendment. Ordinary implementation choices are engineering decisions/tickets.

Do not resurrect superseded designs such as 10×20 production, two-query Maps, 73-point production grids, AIO 13-point production, weekly Full Panel, one-anchor/four-family ChatGPT, retry-until-positive, destructive overwrite, or universal cross-surface visibility scores.

## Governing PRDs

- Unified parent PRD: `1Y5CmSWDpSKryfkmcbPh25UG_yfyyvwZV2hdkh1hh3Sc`
- Maps/Organic PRD: `1pP1dKD341vtzBEA5w4H0-YidX3N2CRVr41cyDV8kD_A`
- AIO PRD: `1oDYH3_oYOvjD3g8jtt13QchxM0C8lgT1B_mq68V-5JI`
- ChatGPT PRD: `1YfWjb9gHzMr8uNriEwdQePhygFp-mjuN0C0c1dyvn54`

## Build-sequence progress

- Steps 1–5 **done**: repo reconciled to authoritative artifacts; schema split into ordered migrations; research schemas private + content-addressed raw Storage bucket authored; Manifest v1.0 + QA rules seeded; DB keys/counts reconciled to Manifest v1.0 (1,100 → 1,000/91/9). Migrations `001`–`023` now **applied to the production Supabase project** and re-reconciled there.
- Step 6 **done (Maps + Organic)**: DataForSEO adapter, immutable content-addressed raw, parser → normalizer → entity resolution → cost ledger, deterministic idempotency, providers mocked in tests. Maps place_id-first; Organic URL-first→domain to `core.web_url`/`core.web_domain`. Both validated offline against the real schema (`scripts/validate_spike.py`).
- Step 7 **done on production (2026-09-14)**: the single-coordinate spike ran end-to-end live against the persistent production Supabase project for **both** Maps and Organic, verified by independent Supabase query (see *Stage-1 pilot findings* → "First paid single-coordinate spike on PRODUCTION"). Total paid spend $0.0012.
- Step 8 **harness built + offline-validated (2026-09-14)**: `collector/pilot.py` (matrix iteration + water gate + bounded retries + QA/cost telemetry + wave evaluator) and `scripts/validate_pilot.py`. Concurrency added (PR #15): `--workers` partitions by (industry, market, surface) so a Maps `place_id` — local to one cell — is never created by two workers at once, and Organic web-entity creation is race-safe (single-key advisory lock + UNIQUE-constraint/SAVEPOINT recovery). Provider-error capture + worker-fault resilience added (PR #16).
- Step 8 **EXECUTED on production + Step 9 COMPLETE (2026-09-14)**: the bounded 3×5 pilot ran live on the production Supabase project (wave `PILOT-3x5-20260914`, DataForSEO `DFS_MAPS_V2` 14z + Organic), gated on owner go. Final QA/Wave-Acceptance v0.1 evaluation = **COMPLETE**: **1,368/1,368 executable returned (100%)**, 192 structurally excluded, **valid-obs 1.0 overall / Maps 1.0 / Organic 1.0**, raw-integrity 1.0, normalization 1.0, resolution-coverage 1.0, manifest-conformity 1.0, **all 30 industry×market×surface strata pass**, 0 failed / 0 quarantined. **Total paid spend ≈ $0.8142.** Canonical entity graph: **1,304 businesses** (Maps, place_id-first), **276 web domains + 677 web URLs** (Organic, URL-first→domain); **0 identifier splits / 0 duplicate place_ids / domains / URLs** — concurrency was entity-safe on real data. Run mechanics validated: idempotency/resume (a run resumed the same wave without re-paying collected jobs), the water gate (`blocked_structural`, never submitted), and the cost ledger reconciled against the seeded price (migration 023). One transient pocket surfaced and self-healed: 7 Organic jobs in IND022×MKT040 hit a DataForSEO `ProviderError` (slow/stuck async task) and drove an intermediate FAILED evaluation (stratum floor); a targeted resume re-collected all 7 on retry → COMPLETE. PRs #15/#16 hardened the harness against this class of issue (fault-resilient parallel run + captured provider-error detail).

## Immediate next action

Steps 1–9 are **done** (Stage-1 pilot **COMPLETE** and production-promotable). **Step 10 — Full Panel / Sentinel operation for Maps + Organic — is COMPLETE** (first monthly Full Panel ran on production and evaluated COMPLETE), governed by **ADR-0007** + `docs/design/full-panel-sentinel-scheduler-v0_1.md` (no silent methodology drift). **Stage 2 (AIO) BUILD is COMPLETE** (schema + `parse_aio` normalizer + gated `aio_driver`/`RUN_PAID_AIO` + cost-reconciliation fix, all merged to `main` + offline-validated; see the *Stage 2 (AIO)* section above).

> **UPDATE (2026-09-18): the graduated first paid AIO wave is DONE — see the top *Latest* section.** Wave `AIO-20260918` ran on production (`RUN_PAID_AIO=1`, re-closed), 30/30 collected, QA COMPLETE, **$0.0384**; the `load_async_ai_overview` add-on measured at ~**1,280 µUSD/task avg (~2.1× the 600 µUSD SERP base)**. The Maps/Organic Full Panel was also re-run on the new `GEOGRID13E_V1` grid (`FULLPANEL-202609-G13E`, 119,000/119,000, COMPLETE, $70.63). **Original instructions retained below for reference.** Next AIO scope decision (own go/no-go): widen to the full **13-point × 10-condition** panel via `AIO_ALL_CONDITIONS=1` / `AIO_POINTS_FULL=1` (~148,750 executable at the measured ~1,280 µUSD/task). Note the flag is now `--points-full` / `AIO_POINTS_FULL` (13-point GEOGRID13E_V1), not the retired `AIO_POINTS_9`.
>
> **(Original, now historical) IMMEDIATE NEXT ACTION — the graduated first paid AIO wave.** To run it: (1) confirm `main` is deployed to the Railway `production` service; (2) set `RUN_PAID_AIO=1` and redeploy (leave every other paid gate `0`) — runs `python -m collector.aio_driver --execute --persist-evaluation` over the GRADUATED default scope (30 executable tasks, `AIO-<YYYYMMDD>` wave); (3) **re-close the gate** (`RUN_PAID_AIO=0`) after. Resume without re-paying via `AIO_RESUME=1` or `AIO_WAVE_CODE`.

Step 10 progress (all done):

1. **Design signed off + merged** (PR #18): ADR-0007 + the scheduler design doc. Key decisions: Sentinel is a fixed *selection* over the frozen universe (the monthly Full Panel doubles as that week's Sentinel — no separate collection, no double-pay); adopt DataForSEO's Standard **decoupled** method (batched `task_post` ≤100/POST → `tasks_ready`/`task_get` collect + reconcile) for throughput; reuse the validated harness end to end; a distinct closed `RUN_PAID_PANEL` gate; graduated first live step is one **Sentinel** wave, not a cold Full Panel. In-scope executable counts (from the production manifest): **Full Panel 118,000** Maps+Organic jobs/month (~$70.80), **weekly Sentinel 4,400** (~$2.64).
2. **Build step 1 merged** (PR #19): manifest-driven wave generator + set-based dry-run accounting (`collector/panel.py`). Offline-validated: Full Panel 130,000 planned → 118,000 executable / 12,000 excluded; Sentinel 5,200 → 4,400 / 800; v0.7 order; Sentinel a strict subset of the Full Panel. No paid call.
3. **Build step 2 merged** (PR #20): Standard decoupled adapter + two-phase panel runner (`collector/dataforseo.py` batch methods, `collector/panel_run.py`); the validated scientific back half is shared via `spike.finalize_collected`. Offline-validated: submit→collect→reconcile, one paid task per job, idempotent resume that re-POSTs nothing, QA COMPLETE / FAILED-not-QUARANTINED. `run_spike` behavior unchanged. No paid call.
4. **Build step 3 built + offline-validated + merged (PR #22):** the cadence driver (`collector/panel_driver.py`) + the `RUN_PAID_PANEL` gate + Railway wiring (`scripts/railway_run.sh`, `PANEL_*` vars). **Step 4 (live) EXECUTED:** the graduated first paid Sentinel wave `SENTINEL-2026W38` ran on production and evaluated **COMPLETE** (~$2.64); `RUN_PAID_PANEL` re-closed.
5. **Full Panel EXECUTED + COMPLETE (2026-09-16):** on explicit owner "go", the first monthly Full Panel `FULLPANEL-202609` ran on production (`RUN_PAID_PANEL=1`, `PANEL_KIND=full_panel`, then re-closed to `0`) and evaluated **COMPLETE**: 130,000 planned → **117,994/118,000 executable returned (99.995%)** / 12,000 structurally excluded, entity graph clean (0 duplicate domains/URLs), 0 real provider failures, 6 tasks accounted `collect_timeout` (paid at submit, uncollected — within the COMPLETE tolerance), **$70.29** realized. Two improvements shipped while running it (no methodology change, zero re-pay): **parallel collect phase** (cell-affinity worker pool partitioned by industry×market×surface + sharded, order-safe web-entity create locks; PR #24) and **direct `task_get`-by-stored-id collection** (PR #25) — the first paid resume ran ~19h after submit, and DataForSEO's `tasks_ready` roster had aged the completed tasks off within hours, stalling collection at 80,780; fetching each outstanding task by its stored `provider_task_id` (retrievable ~30 days) is roster-independent and drained the rest. Both offline-validated (pytest 78 + validators incl. a 4-worker concurrent run and a roster-aged regression). All paid gates re-closed (`0`).
6. **AIO (Stage 2) BUILD COMPLETE** (offline; PRs #34/#36/#37 merged) — the graduated first paid AIO wave is the immediate next action above. **ChatGPT (Stage 3) stays behind its own ADR-0005 capture probe.** Enrichment / findings / Client Mode remain deferred.
7. **Optional harness follow-ups (non-blocking):** submit-phase re-scan is single-threaded (~15–25 min per resume of the full universe) — parallelize or make resume skip-scan; raise parallel-collect throughput (per-task Supabase-Storage PUT + DB writes cap it near ~2–4× rather than the worker count); DataForSEO postback/pingback to push ready tasks; mop up the 6 `collect_timeout` tasks on a later resume if a literal 118,000 is wanted. (The Full-Panel web-entity lock-sharding follow-up is DONE — PR #24; the `tasks_ready`-polling dependency is removed — PR #25.)

**Outstanding owner cleanup:** none blocking. (The stray Railway service `zucchini-ambition` was deleted; the production DB password AND `service_role` key were rotated on 2026-09-14 and the redeploy verified green.)

### Pilot harness (step 8) — built, gated, offline-validated

- **`collector/pilot.py`** — `expand_matrix` (1,560 pre-water, job-generator v0.7 order), `PilotRunner` (water gate → `run_spike` per executable job, per-job commit/resume, bounded retries for transport/provider-infra only, structural coordinates recorded as `blocked_structural` provenance and never submitted), and `evaluate_wave` (reads the wave back; emits COMPLETE/PARTIAL/FAILED/QUARANTINED under QA/Wave-Acceptance v0.1 + a separate financial block; persists `ops.wave_evaluation` + `ops.qa_event`).
- **Gating:** nothing calls a provider unless `--execute` is passed; the Railway path additionally requires `RUN_PAID_PILOT=1`. `--dry-run` (or omitting `--execute`) prints the water-gated accounting with no writes/calls.
- **Offline validation:** `scripts/validate_pilot.py` runs the whole 1,560-job matrix through fake providers on an ephemeral pgvector schema — asserts 1,560 → 1,368/192 accounting, no observation/cost on excluded coordinates, idempotent full re-run, QA **COMPLETE** on a clean wave and **FAILED** on a provider-failure wave (integrity intact). `tests/test_pilot.py` covers matrix order, the retry classifier, and the QA status ladder. `pytest` = 48 tests, all providers mocked; no paid call.
- **PARTIAL thresholds (step-9 acceptance):** ≥99.5% overall terminal coverage, ≥99.0% per surface, ≥95% per industry×market×surface stratum (≥20 executable jobs), no critical integrity failure. Production promotion requires COMPLETE.

### Panel scheduler (step 10) — COMPLETE: built + offline-validated; first paid Sentinel wave COMPLETE; first monthly Full Panel COMPLETE

- **`collector/panel.py`** — manifest-driven `generate_wave_specs(kind=full_panel|sentinel)` (reuses `pilot.expand_matrix` in job-generator v0.7 order) + set-based `plan_wave` dry-run (water-gated accounting + cost estimate). Plans only; `--execute` refused. `scripts/validate_panel.py` asserts the 118,000/12,000 and 4,400/800 splits, v0.7 order, set-based == per-spec water gate, Sentinel ⊂ Full Panel, cost estimate — no paid call.
- **`collector/panel_run.py`** — `PanelRunner` (submit → collect → reconcile) on the Standard decoupled adapter (`dataforseo.task_post_batch` ≤100/POST); idempotency at submission (one paid task per job, collect-only on resume, never re-POSTed); reuses the water gate, `spike.finalize_collected`, cost ledger, and `pilot.evaluate_wave`. **Collect is parallel (PR #24)** — a bounded worker pool partitioned by (industry, market, surface) so a Maps `place_id` is single-writer per cell, with the repository's ordered per-domain web-entity pre-lock keeping Organic creation deadlock-free (`PANEL_WORKERS` / `--workers`, default 1). **Collection fetches each task by its stored `provider_task_id` directly (PR #25)** rather than off the `tasks_ready` roster (which ages tasks out within hours), so resume-after-delay works. `scripts/validate_panel_run.py` asserts a clean run (QA COMPLETE), a 4-worker concurrent run (0 duplicate web_domain/web_url), a roster-aged run (empty `tasks_ready` still collects all via direct fetch), an idempotent resume that re-POSTs nothing, and reconcile (never-ready → accounted collect_timeout → QA FAILED, not QUARANTINED) — no paid call. Providers mocked in tests.
- **`collector/panel_driver.py` (step 3) — built + offline-validated, gated.** The thin cadence driver: decides the wave kind (`--kind auto` → **Full Panel on the monthly anchor** — first scheduled run of a calendar month with no `full_panel` wave yet — else **Sentinel**; declarative over DB state, resume-safe, no cadence-anchor table), resolves a deterministic per-period wave code (`FULLPANEL-<YYYYMM>` / `SENTINEL-<ISOyear>W<ISOweek>`, so a same-period re-invocation resumes the same wave — idempotent, no re-pay; `--wave-code` / `--resume` override), then runs `generate_wave_specs → PanelRunner → evaluate_wave` (persisted) and surfaces the QA status (anything below COMPLETE exits non-zero — **no silent partials**). **Gate: a NEW `RUN_PAID_PANEL` (default `0`/closed, independent of `RUN_PAID_SPIKE`/`RUN_PAID_PILOT`)** — the paid `--execute` path refuses unless `RUN_PAID_PANEL=1`, checked before any DB connection; `--dry-run` (default) prints the set-based plan for the resolved kind (no writes, no call). `scripts/railway_run.sh` now dry-runs the panel plan on deploy and runs the paid driver only when `RUN_PAID_PANEL=1` (new `PANEL_KIND`/`PANEL_WAVE_CODE`/`PANEL_RESUME`/`PANEL_BATCH_SIZE`/`PANEL_POLL_INTERVAL`/`PANEL_COLLECT_TIMEOUT` vars). `scripts/validate_panel_driver.py` drives the driver end to end through a fake batch provider on ephemeral pgvector (kind decision, deterministic codes, an `auto` Full Panel then `auto` Sentinel run with the right `wave_kind`/`panel_subset_id` → QA COMPLETE, idempotent resume that re-POSTs nothing) — no paid call. `tests/test_panel_driver.py` covers the decision logic + `RUN_PAID_PANEL` refusal DB-free. `pytest` = 78 passing.
- **Step 4 (live) — EXECUTED + COMPLETE.** The graduated first paid Sentinel wave ran on production via the Railway service (`RUN_PAID_PANEL=1`, `PANEL_KIND=sentinel`, then re-closed to `0`): wave **`SENTINEL-2026W38`** (Standard decoupled), **5,200 planned → 4,400/4,400 executable returned (100%) / 800 structurally excluded**, all 4,400 tasks submitted (one paid task per job), 0 terminal failures. QA/Wave-Acceptance v0.1 = **COMPLETE**: job-accounting/valid-observation (maps + organic)/raw-integrity/normalization-parity/resolution-coverage/manifest-conformity all **1.0**, **all 100 strata pass**, 0 failed / 0 quarantined / 0 critical QA events; entity graph clean (0 duplicate domains / URLs). **Total paid spend $2.6304** (4,400 × 600 µUSD, exactly the estimate). Guardrails held: immutable raw before normalization, structural coords never submitted, idempotency-at-submission (4,400 submitted = 4,400 executable).
- **Full Panel (live) — EXECUTED + COMPLETE (2026-09-16).** On explicit owner "go", the first monthly Full Panel `FULLPANEL-202609` ran on production via the Railway service (`RUN_PAID_PANEL=1`, `PANEL_KIND=full_panel`, `PANEL_WORKERS=8`, then re-closed to `0`): **130,000 planned → 118,000 executable submitted (one paid task per job) / 12,000 structurally excluded**; **117,994/118,000 collected (99.995%)**, 6 accounted `collect_timeout` (paid at submit, uncollected). QA/Wave-Acceptance v0.1 = **COMPLETE**; entity graph clean (**0 duplicate domains / URLs**), 0 real provider failures. **Total realized spend $70.29** (cost ledger; ~596 µUSD/task). The run surfaced + fixed a resume-robustness bug live: after switching to the parallel collector ~19h post-submit, the `tasks_ready` roster had aged the completed tasks off and collection stalled at 80,780 — PR #25 (direct `task_get`-by-id) drained the remainder with zero re-pay. Guardrails held throughout: immutable raw before normalization, structural coords never submitted, idempotency-at-submission, no methodology/geometry/depth change.
- **Step 10 is COMPLETE.** All paid gates re-closed (`0`). Next paid work (a subsequent monthly Full Panel / weekly Sentinel cadence, or AIO/ChatGPT stages behind their ADR-0005 probes) is future scope on explicit owner "go".

**Outstanding owner cleanup:** none blocking (`zucchini-ambition` deleted; prod DB password + `service_role` key rotated + verified green 2026-09-14).

Do not re-derive or re-sign the research architecture.
